#ifndef __Utility__
#define __Utility__

#include <iostream>
#include <fstream>

using namespace std;

int getHighScore(string path);
string modify(int value);
void replaceHighScore(int score, string path);

#endif